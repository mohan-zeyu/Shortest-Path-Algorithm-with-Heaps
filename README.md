The heap implementations are all in the corresponding directories.
The `generate.c` file is used to generate random queries in `Queries/` directory.
And the shortest path length will be stored in `Results/`

Use `gcc -O3 ".c files names" -o main` to compile the program in each directory.